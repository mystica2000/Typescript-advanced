type positive = boolean;
const isCat:positive = true;
// alias 

console.log(isCat)
console.log(typeof isCat) // boolean not positive since it is just a alias

// type cannot be extended but can used with "|" or "&"
